[webservers] # This is the group name used in the playbook (hosts: your_target_hosts)
192.168.68.51 # <-- REPLACE with the actual IP address or hostname of your Ubuntu server

# If your SSH username on the target server is different from your username on the Ansible control machine,
# you can specify it like this:
# your_server_ip_or_hostname ansible_user=your_ssh_username
